import * as Joi from 'joi';

export const configSchema = Joi.object({
  // PORT: Joi.required(),
  // BD_HOST: Joi.required(),
  // BD_PORT: Joi.required(),
  // BD_DATABASE: Joi.required(),
  // BD_USERNAME: Joi.required(),
  // BD_PASSWORD: Joi.required(),
});
